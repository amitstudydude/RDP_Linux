
name: CI
on: 
  workflow_dispatch:
   inputs:
     auth:
        description: 'Code'
        required: true
        default:
  
defaults:
  run:
    shell: bash
jobs:
  build:
    ##runs-on: ubuntu-latest
    runs-on: self-hosted 
    steps:     
    - name: Start
      run: |
           
