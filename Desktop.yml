
name: main
on: 
  workflow_dispatch:
   inputs:
     auth:
        description: 'Code'
        required: true
        default:
  
defaults:
  run:
    shell: bash
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v1
    - name: Creating User to Login
      run: |
           sudo useradd -m user && sudo adduser user sudo && echo 'user:user' | sudo chpasswd
           sudo usermod -a -G sudo,adm user
           
            
          
    - name: Start Chrome Remote Desktop
      run: |
           
           wget https://dl.google.com/linux/direct/chrome-remote-desktop_current_amd64.deb
           sudo apt install --assume-yes --fix-broken ./*.deb && rm *.deb
           ${{ github.event.inputs.auth }} -pin=123456
           sudo apt install firefox iputils-ping -y
           rm *.zip *.zip.*
           wget https://bin.equinox.io/c/4VmDzA7iaHb/ngrok-stable-linux-amd64.zip 
           unzip ./*.zip
           ./ngrok authtoken 286ptBCnj3Na7gOHdtMVxxCvXoF_5cvEELBfwS1FDm4eWcSG3
           echo https://dashboard.ngrok.com/cloud-edge/endpoints 
           ./ngrok 
