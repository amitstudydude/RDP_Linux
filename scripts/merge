export owner="amitstudydude"
la
rm -rf *
git clone https://github.com/$owner/$repo.git
rsync -a "$repo"/* . 
rm -rf "$repo"
date > .github/workflows/README.md && sleep 1
git config --global user.name 'amitstudydude'
git config --global user.email 'amitstudydude@users.noreply.github.com'
git pull
git add -A
git commit -am "merge"
git push origin main

 
git remote add upstream https://github.com/$owner/$repo.git     
git fetch upstream             
git checkout main         
git merge --no-edit upstream/main       
git push origin main

## export owner="amitstudydude" && export repo=${{ github.event.repository.name }} && curl https://raw.githubusercontent.com/jha-ji/RDP_Linux/main/scripts/merge | bash


#
